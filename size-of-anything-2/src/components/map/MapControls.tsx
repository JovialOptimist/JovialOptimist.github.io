import React from 'react';

/**
 * Controls for the map view (zoom, layers, etc.)
 */
const MapControls: React.FC = () => {
  return (
    <div className="map-controls">
      {/* Map control buttons will go here */}
    </div>
  );
};

export default MapControls;